# scripts

Place shared PowerShell and other automation here when the scripts are intended to operate across repositories under `D:\code`.

Prefer scripts that resolve paths relative to the `D:\code` root so they work consistently from this umbrella repository.
